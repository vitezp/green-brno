import { motion } from 'framer-motion'
import styled from '@emotion/styled'

export const SContainer = styled(motion.svg)`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) rotate(-90deg);
`
