import { size } from 'theme'

export const R_COVER = size(54)
export const R_GAUGE = size(66)

export const SVG_WIDTH = 2 * 4 * 60
export const SVG_RADIUS = 0.25 * SVG_WIDTH
export const SVG_CIRC = 2 * Math.PI * SVG_RADIUS
